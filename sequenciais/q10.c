#include <stdio.h>
#include <math.h>
int main(int argc, char ** argv) {
  printf("Digite um número inteiro");
  scanf("%d\n", &a);
  printf(a>0?"A raiz do numero é %f", sqrt(a):"O numero ao quadrado é: %f", pow(x,2));
  return 0;
}
